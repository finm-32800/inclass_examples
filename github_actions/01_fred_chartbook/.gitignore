# Everything here is regenerated by `doit`. The repo-root .gitignore already
# covers _data/ and _output/, but this local file keeps the example
# self-contained if you copy the directory into a fresh repository.
_data/
_output/
docs/
.doit-db.sqlite
__pycache__/
