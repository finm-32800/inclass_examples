# Everything here is regenerated by `doit`. The repo-root .gitignore already
# covers _output/, but this local file keeps the example self-contained if
# you copy the directory into a fresh repository.
_output/
.doit-db.sqlite
__pycache__/
